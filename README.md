# TeleBotOrder
This is a bot in telegram ,where people can anonymously place an order or sell something.



# Linux / macOS
python3 -m venv venv
source venv/bin/activate

# Windows (PowerShell)
python -m venv venv
venv\Scripts\Activate

pip install -r requirements.txt



for developers:
    after "pip install/uninstall dependen" use pip freeze > requirements.txt. 